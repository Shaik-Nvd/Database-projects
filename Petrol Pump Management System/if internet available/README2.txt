		
		
		
		
		
				This project is used in a local server(sysetem) and it can be intracted through MySQL
					this process is done in a linux command line interface (Terminal)
				       use the following commands to invoke and use the software's database
				       
				       --mysql -u root -p
				       --`password enterd`
				       --use `database-name`
				      
